import { defineAppSetup } from '@slidev/types'

export default defineAppSetup(() => {
  // Intentionally minimal.
  // Slidev auto-discovers components/layouts/styles from the theme package,
  // so we keep this hook as a documented extension point for future setup needs.
})

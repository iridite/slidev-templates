import { defineAppSetup } from '@slidev/types'

export default defineAppSetup(({ app, router }) => {
  // Global component registration happens automatically via Slidev's component discovery
  // This file can be used for additional app-level setup if needed
})

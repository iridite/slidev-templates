name: Registry Health

on:
  workflow_dispatch:
  push:
    paths:
      - 'registry/**'
      - 'scripts/check-registry-health.mjs'
      - '.github/workflows/registry-health.yml'
  pull_request:
    paths:
      - 'registry/**'
      - 'scripts/check-registry-health.mjs'
      - '.github/workflows/registry-health.yml'
  schedule:
    - cron: '17 4 * * 0'

permissions:
  contents: read

concurrency:
  group: registry-health-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 20

      - name: Check canonical sources and previews
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node scripts/check-registry-health.mjs
